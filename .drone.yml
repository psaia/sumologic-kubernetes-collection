---
    kind: pipeline
    type: docker
    name: default
    
    
    steps:
      - name: format
        image: hashicorp/terraform:0.12.26
        commands:
            - cd collectors
            - terraform fmt -write=false -list -diff -check
        when:
            event: [push, pull_request]